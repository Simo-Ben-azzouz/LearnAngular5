import { TestDerectiveDirective } from './test-derective.directive';

describe('TestDerectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TestDerectiveDirective();
    expect(directive).toBeTruthy();
  });
});
